[pytest]
# Pytest configuration for CEREBRO-RED v2 tests

# Set Python path to backend directory (parent of tests/)
pythonpath = ..

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Async support
asyncio_mode = auto

# Markers for test categorization
markers =
    e2e: End-to-end tests that require real LLM providers
    benchmark: Performance benchmark tests
    slow: Tests that take a long time to run
    integration: Integration tests with mocked dependencies
    cors: CORS-related tests
    auth: Authentication and authorization tests

# Test output options
addopts =
    -v
    --tb=short
    --strict-markers
    --disable-warnings

# Coverage options (if pytest-cov is installed)
# [tool:pytest]
# addopts = --cov=core --cov=utils --cov=api --cov-report=html --cov-report=term-missing


# ============================================================================
# CEREBRO-RED v2 Frontend Environment Variables
# ============================================================================
# Copy this file to .env for local development:
#   cp .env.example .env
# ============================================================================

# Demo Mode Configuration
# ------------------------
# Set to 'true' to enable demo mode (read-only with mock data)
# When enabled, all API calls are routed to /api/demo/* endpoints
# Default: false (normal operation)
VITE_DEMO_MODE=false

# API Configuration (Optional)
# -----------------------------
# Base URL for API requests
# Leave empty to use Vite proxy in development (proxies to localhost:9000)
# Set for production deployments or non-standard backend ports
# Example: VITE_API_BASE_URL=https://api.cerebro-red.com
# Default: '' (uses Vite proxy)
# VITE_API_BASE_URL=

# Production API Configuration
# -----------------------------
# Used in production Docker builds (docker-compose.prod.yml)
# HTTP API endpoint
VITE_API_URL=http://localhost:9000
# WebSocket endpoint for real-time updates
VITE_WS_URL=ws://localhost:9000

# Note: For production deployments, update these to your actual backend URLs:
# VITE_API_URL=https://api.your-domain.com
# VITE_WS_URL=wss://api.your-domain.com

# ============================================================================
# Additional Configuration
# ============================================================================
# Add any additional environment variables below as needed for your deployment
